; -------------------- Energy Calculator --------------------

[balance / energy / energyCalculator]
; Disable source tier limiter at runSyncStep(Lic2/core/energy/grid/EnergyNetLocal;)Z
; (Default: false)
disableSyncSourceTierLimit = false

; Disable source tier limiter at distributeMultiple
; (Default: false)
disableDistributeSourceTierLimit = false

; -------------------- Block: Generators --------------------

; Geo-thermal Generator
[balance / generator / geothermal]
; Geo-thermal generator energy storage capacity (Default: 2400)
energyCapacity = 2400

; Geo-thermal generator fluid tank capacity (Default: 8000)
fluidTankCapacity = 8000

; Semi-fluid Generator
[balance / generator / semiFluid]
; Semi-fluid generator energy storage capacity (Default: 32000)
energyCapacity = 32000

; Semi-fluid generator fluid tank capacity (Default: 10000)
fluidTankCapacity = 10000

; ----------- Semi-fluid Generator fuels -----------
[balance / generator / semiFluid / oil]
; (Default: 16)
energyPerMb = 16
; (Default: 8)
energyPerTick = 8

[balance / generator / semiFluid / fuel]
; (Default: 128)
energyPerMb = 128
; (Default: 32)
energyPerTick = 32

[balance / generator / semiFluid / biomass]
; (Default: 8)
energyPerMb = 8
; (Default: 8)
energyPerTick = 8

[balance / generator / semiFluid / bioethanol]
; (Default: 128)
energyPerMb = 128
; (Default: 32)
energyPerTick = 32

[balance / generator / semiFluid / biogas]
; (Default: 32)
energyPerMb = 32
; (Default: 16)
energyPerTick = 16

[balance / generator / semiFluid / creosote]
; (Default: 3)
energyPerMb = 3
; (Default: 8)
energyPerTick = 8

; -------------------- Block: Machines --------------------

; ---------- Liquid Heat Exchanger ----------
[balance / liquidHeatExchanger]
; Liquid Heat Exchanger maximum heat exchange(s) per tick
maxHeatExchangePerTick = 100

; Liquid Heat Exchanger input fluid tank capacity (Default: 2000)
inputTankCapacity = 2000

; Liquid Heat Exchanger output fluid tank capacity (Default: 2000)
outputTankCapacity = 2000

; ----------  Steam Generator ----------
; Note: hu_need = baseHUNeed + Math.round(pressureValve / huNeedRatio * huNeedMultiplier)
[balance / steamGenerator]
; Steam Generator maximum requested HU when heating up (Default: 1200)
baseHeatupHU = 1200

; Steam Generator base HU need (Default: 100)
baseHUNeed = 100

; Steam Generator HU need ratio (Default: 220.0)
huNeedRatio = 220.0

; Steam Generator HU need multiplier (Default: 100.0)
huNeedMultiplier = 100.0

; Enables Steam Generator calcification (Default: true)
enableCalcification = true

; ----------  Nuclear Reactor ----------
[balance / nuclearReactor]
; Nuclear Reactor coolant tank capacity (Default: 10000)
inputTankCapacity = 10000

; Nuclear Reactor hot-coolant tank capacity (Default: 10000)
outputTankCapacity = 10000

; Nuclear Reactor gui tick-rate (Default: 20)
tickRate = 20

; ---------- Recycler ----------
[balance / recycler]
; Recycler recycle chance (Default: 8)
; Lower value = better chance
recycleChance = 8

; Recycler operation length (Default: 45)
; Lower value = operation finished faster
operationLength = 45

; ----------  Blast Furnace ----------
[balance / machine / blastFurnace]
; Blast Furnace progress_needed *PRE* offset (applied BEFORE multiplier applies)
; (Default: 0)
progressNeededPreOffset = 0

; Blast Furnace progress_needed multiplier (Default: 1.0)
progressNeededMultiplier = 1.0

; Blast Furnace progress_needed *POST* offset (applied AFTER multiplier applies)
; (Default: 0)
progressNeededPostOffset = 0

; ---------- Solar Destiller ----------
[balance / solarDestiller]
; Solar Destiller process unit (Mb/tickrate)
 ; (Default: 1)
processUnit = 1

; Solar Destiller base operation tick-rate (Default: 72)
; Lower value = operation finished faster
defaultTickrate = 72

; Solar Destiller operation tick-rate in hot biomes (such as Desert) (Default: 36)
; Lower value = operation finished faster
hotBiomeTickrate = 36

; Solar Destiller operation tick-rate in cold biomes (such as Ice Plains) (Default: 144)
; Lower value = operation finished faster
coldBiomeTickrate = 144

; ---------- Condenser ----------
[balance / condenser]
; Condenser passive cooling amount (Default: 100)
passiveCooling = 100

; Condenser active cooling amount per vent (Default: 100)
activeCoolingPerVent = 100

; ---------- Advanced Miner ----------
[balance / advMiner]
; Adv. Miner working tick rate (Default: 20)
workTick = 20

; Adv. Miner energy consume when scanning (Default: 64)
scanEnergy = 64

; Adv. Miner energy consume when mining (Default: 512)
mineEnergy = 512

; ---------- Crop Harvester ----------
[balance / cropHarvester]
; Crop Harvester horizontal(X, Z) range (Default: 4)
horizontalRange = 4

; Crop Harvester vertical(Y) range (Default: 1)
verticalRange = 1

; Crop Harvester tick rate (Default: 10)
tickRate = 10

; ---------- Cropmatron ----------
[balance / cropmatron]
; Cropmatron scan horizontal(X, Z) range (Default: 4)
horizontalRange = 4

; Cropmatron scan vertical(Y) range (Default: 1)
verticalRange = 1

; Crop Harvester tick rate (Default: 10)
tickRate = 10

; ---------- Induction ----------
[balance / induction]
; Induction operation length (Default: 4000)
operationLength = 4000

; ---------- Fermenter ----------
[balance / fermenter]
; Fermenter maximum heat request amount per tick (Default: 100)
heatRequest = 100

; Fermenter input tank capacity (Default: 10000)
inputTankCapacity = 10000

; Fermenter output tank capacity (Default: 2000)
outputTankCapacity = 2000

[balance / generic]

; Canner operation length (Default: 200)
; Lower value = operation finished faster
cannerOperationLength = 200

; Compressor operation length (Default: 300)
; Lower value = operation finished faster
compressorOperationLength = 300

; Electric furnace operation length (Default: 100)
; Lower value = operation finished faster
electricFurnaceOperationLength = 100

; Extractor operation length (Default: 300)
; Lower value = operation finished faster
extractorOperationLength = 300

; Fluid bottler operation length (Default: 100)
; Lower value = operation finished faster
fluidBottlerOperationLength = 100

; Iron furnace operation length (Default: 160)
; Lower value = operation finished faster
ironFurnaceOperationLength = 160

; Macerator operation length (Default: 300)
; Lower value = operation finished faster
maceratorOperationLength = 300

; Metal former operation length (Default: 200)
; Lower value = operation finished faster
metalFormerOperationLength = 200

; Ore washer operation length (Default: 500)
; Lower value = operation finished faster
oreWasherOperationLength = 500

; Pump operation length (Default: 20)
; Lower value = operation finished faster
pumpOperationLength = 20

; Solid canner operation length (Default: 200)
; Lower value = operation finished faster
solidCannerOperationLength = 200

; Centrifuge operation length (Default: 500)
; Lower value = operation finished faster
centrifugeOperationLength = 500

; -------------------- Upgrades --------------------

; ---------- Overclock upgrade ----------
[balance / upgrade / overclock]
; Process time multiplier (Default: 0.7)
; Lower value = operation finished faster
processTimeMultiplier = 0.7

; Energy demand multiplier (Default: 1.6)
; Greater value = demands more EUs
energyDemandMultiplier = 1.6

; ---------- Energy storage upgrade ----------
[balance / upgrade / energyStorage]
; Extra energy storage capacity increment (Default: 10000)
extraEnergyStorage = 10000

; -------------------- Items --------------------

; ---------- Nano saber ----------
[balance / tool / nanosaber]
; Nano saber block break speed when turned on (Default: 4.0)
; Greater value = break blocks faster
digSpeed = 4.0

; Nano saber web break speed when turned on (Default: 50.0)
; Greater value = break blocks faster
webDigSpeed = 50.0

; ---------- Mining laser ----------
[balance / tool / miningLaser]
; Mining laser power multiplier (Default: 1.0)
; Greater value = count of mined blocks per a laser shot increased
powerMultiplier = 1.0

; Mining laser range multiplier (Default: 1.0)
; Greater value = base laser range increases
rangeMultiplier = 1.0

; ---------- Electric Item ----------
[balance / electricItem]
; Ignore energy transfer limit when charging electric item(s) (Default: false)
ignoreChargeTransferLimit = false

; Ignore energy transfer limit when discharging electric item(s) (Default: false)
ignoreDischargeTransferLimit = false

; ---------- Crop ----------
[balance / crop / humidity]
; Crop humidity biome bonus
; true = Bonus amount affected by biome
; false = Bonus amount fixed to 10
; (Default: true)
biomeBonusCheck = true

; Crop humidity farmland wetness bonus
; true = Only give bonus when the farmland is wet
; false = Always give bonus
; (Default: true)
farmlandWetnessBonusCheck = true

[balance / crop / nutrients]
; Crop nutrients biome bonus
; true = Bonus amount affected by biome
; false = Bonus amount fixed to 10
; (Default: true)
biomeBonusCheck = true
; Crop nutrients below dirt bonus
; true = Bonus amount affected by count of dirt block(s) below the crop
; false = Bonus amount fixed to 3
; (Default: true)
belowDirtBonusCheck = true

[balance / crop / airQuality]
; Crop air quality height bonus
; true = Bonus amount affected by y coord of crop
; false = Bonus amount fixed to 4
; Note: bonus = ((yCoord - 64) / 15).clamp(0, 4)
; (Default: true)
heightBonusCheck = true

; Crop air quality freshness bonus
; true = Bonus amount affected by nearby blocks
; false = Bonus amount fixed to 9
; Note:
;   fresh = 9
;   for(int x = this.xCoord - 1; x <= xCoord + 1 && fresh > 0; ++x)
;       for(int z = this.zCoord - 1; z <= zCoord + 1 && fresh > 0; ++z)
;           if (world.isBlockNormalCubeDefault(x, yCoord, z, false)
;               || world.getTileEntity(x, yCoord, z) instanceof TileEntityCrop)
;                      --fresh
;   bonus += fresh / 2
; (Default: true)
freshnessBonusCheck = true

; Crop air quality see-the-sky bonus
; true = Bonus amount affected by sky availability
; false = Bonus amount fixed to 2
; (Default: true)
canSeeTheSkyBonusCheck = true

; -------------------- Blocks --------------------

; ---------- Foam ----------
[balance / block / foam]
; Foam harden chance multiplier
; (Default: 4096.0)
hardenChanceMultiplier = 4096.0

; Base light reverse-multiplier for foam hardening
; Formula: avgTime = hardenTime * (<baseLightReverseMultiplier> - light);
; (Default: 16)
baseLightReverseMultiplier = 16

[balance / block / foam / normal]
; Normal foam base harden time
; (Default: 300)
hardenTime = 300

[balance / block / foam / reinforced]
; Reinforce dfoam base harden time
; (Default: 600)
hardenTime = 600

; -------------------- Wind --------------------

[balance / wind]

; Default wind strength multiplier (Default: 2.4)
defaultMultiplier = 2.4

; Wind strength multiplier when raining (Default: 1.25)
rainMultiplier = 1.25

; Wind strength multiplier when storming (= thundering) (Default: 1.5)
stormMultiplier = 1.5

; -------------------- Reactor things --------------------

; --- Reactor vent ---
[balance / reactor / heatVent]
; (Default: 1000)
heatStorage = 1000

; (Default: 6)
selfVent = 6

; (Default: 0)
reactorVent = 0

[balance / reactor / reactorHeatVent]
; (Default: 1000)
heatStorage = 1000

; (Default: 5)
selfVent = 5

; (Default: 5)
reactorVent = 5

[balance / reactor / overclockedHeatVent]
; (Default: 1000)
heatStorage = 1000

; (Default: 20)
selfVent = 20

; (Default: 36)
reactorVent = 36

[balance / reactor / componentHeatVent]
; (Default: 4)
sideVent = 4

[balance / reactor / advancedHeatVent]
; (Default: 1000)
heatStorage = 1000

; (Default: 12)
selfVent = 12

; (Default: 0)
reactorVent = 0

[balance / reactor / heatExchanger]
; (Default: 2500)
heatStorage = 2500

; (Default: 12)
switchSide = 12

; (Default: 4)
switchReactor = 4

[balance / reactor / reactorHeatExchanger]
; (Default: 5000)
heatStorage = 5000

; (Default: 0)
switchSide = 0

; (Default: 72)
switchReactor = 72

[balance / reactor / componentHeatExchanger]
; (Default: 5000)
heatStorage = 5000

; (Default: 36)
switchSide = 36

; (Default: 0)
switchReactor = 0

[balance / reactor / advancedHeatExchanger]
; (Default: 10000)
heatStorage = 10000

; (Default: 24)
switchSide = 24

; (Default: 8)
switchReactor = 8
